apiVersion: v1
kind: Service
metadata:
  name: {{ include "maze.fullname" . }}
  labels:
    {{- include "maze.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maze.selectorLabels" . | nindent 4 }}
